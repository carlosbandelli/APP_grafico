version: 1
applications:
  - appRoot: /
    frontend:
      phases:
        preBuild:
          commands:
            - yarn install
        build:
          commands:
            - yarn web
      artifacts:
        baseDirectory: /web-build
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
